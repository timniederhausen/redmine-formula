FreeBSD:
  bundler: /usr/local/bin/bundle
  service: redmine
  packages:
    - subversion
    - rubygem-bundler

Debian:
  bundler: /usr/bin/bundle
  service: redmine
  packages:
    - subversion
    - build-essential
    - ruby2.3
    - ruby2.3-dev
    - ruby-bundler
